name: RDP via Ngrok (Windows)

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 360
    steps:
      - name: Enable Remote Desktop and Firewall
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0 -Force
          netsh advfirewall firewall add rule name="RDP-Ngrok" dir=in action=allow protocol=TCP localport=3389
          Restart-Service -Name TermService -Force

      - name: Create RDP user with fixed password
        run: |
          $pwd = "Dberly1312@2026"
          $secure = ConvertTo-SecureString $pwd -AsPlainText -Force

          if (-not (Get-LocalUser -Name "RDP" -ErrorAction SilentlyContinue)) {
            New-LocalUser -Name "RDP" -Password $secure -AccountNeverExpires -PasswordNeverExpires
            Write-Host "Created new RDP user"
          } else {
            Set-LocalUser -Name "RDP" -Password $secure
            Write-Host "Updated RDP user password"
          }

          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP" -ErrorAction SilentlyContinue
          Add-LocalGroupMember -Group "Administrators" -Member "RDP" -ErrorAction SilentlyContinue

          Write-Host "========================================="
          Write-Host "RDP READY!"
          Write-Host "Username: RDP"
          Write-Host "Password: Dberly1312@2026"
          Write-Host "========================================="

      - name: Install and Run Ngrok
        env:
          NGROK_AUTHTOKEN: ${{ secrets.NGROK_AUTHTOKEN }}
        run: |
          Invoke-WebRequest -Uri "https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-windows-amd64.zip" -OutFile "$env:TEMP\ngrok.zip" -UseBasicParsing
          Expand-Archive -Path "$env:TEMP\ngrok.zip" -DestinationPath "$env:TEMP" -Force
          & "$env:TEMP\ngrok.exe" config add-authtoken $env:NGROK_AUTHTOKEN
          & "$env:TEMP\ngrok.exe" tcp 3389

      - name: Print connection info
        run: |
          Write-Host "========================================="
          Write-Host "RDP READY!"
          Write-Host "Username: RDP"
          Write-Host "Password: Dberly1312@2026"
          Write-Host ""
          Write-Host "Ngrok is running in the background."
          Write-Host "Look at the job log above for the line that starts with 'tcp://'"
          Write-Host "Example: tcp://0.tcp.ngrok.io:12345"
          Write-Host "Use that address in Remote Desktop to connect."
          Write-Host "========================================="

      - name: Keep alive
        run: while ($true) { Start-Sleep -Seconds 300 }
        shell: powershell
